/*Groups.prototype.handle_click_prev = async function(conv_id) {
	await super.handle_click_prev(conv_id)
	
	this.lmem.innerHTML = ''
	Object.entries(this.previous[conv_id].members).forEach(([user_id, v]) => {
		this.add_item_member(user_id, v)
	})
	if (Object.keys(this.previous[conv_id].members) == 1) {
		// show label of leave and delete and image of delete
	}
}*/
